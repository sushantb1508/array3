t=int(input())
for i in range(t):
    n1,n2,n3 = map(int,input().split())
    x= set(map(int,input().split()))
    y= set(map(int,input().split()))
    z= set(map(int,input().split()))
    g=x.intersection(y).intersection(z)
    g=list(set(g))
    if len(g)<=0:
        print(-1)
    else:
        print(*g)
    
    
